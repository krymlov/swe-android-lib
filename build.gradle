plugins {
    id 'com.android.application' version '9.3.1' apply false
    id 'com.android.library' version '9.3.1' apply false
}

allprojects {
    buildDir = "u:/apps/${rootProject.name}"
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
